services:
  - type: web
    name: ai-engineer-academy-backend
    runtime: node

    plan: free

    buildCommand: npm install

    startCommand: npm start

    autoDeploy: true

    envVars:
      - key: NODE_VERSION
        value: 20

      - key: JWT_SECRET
        generateValue: true